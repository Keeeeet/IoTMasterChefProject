name: grill_thermometer
description: "Bluetooth grilový teploměr"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  percent_indicator: ^4.2.3
  fl_chart: ^0.63.0
  flutter_reactive_ble: ^5.0.2
  flutter_local_notifications: ^17.0.0
  shared_preferences: ^2.2.2
  audioplayers: ^5.2.1
  permission_handler: ^11.3.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/beep.mp3

